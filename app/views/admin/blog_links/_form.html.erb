<%= form_for @admin_blog_link, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :title, :class => 'control-label' %><%= mark_required(@admin_blog_link, :title) %>
    <div class="controls">
      <%= f.text_field :title, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :author, :class => 'control-label' %><%= mark_required(@admin_blog_link, :author) %>
    <div class="controls">
      <%= f.text_field :author, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :introduction, :class => 'control-label' %><%= mark_required(@admin_blog_link, :introduction) %>
    <div class="controls">
      <%= f.text_field :introduction, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :url, :class => 'control-label' %><%= mark_required(@admin_blog_link, :url) %>
    <div class="controls">
      <%= f.text_field :url, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                admin_blog_links_path, :class => 'btn' %>
  </div>
<% end %>
